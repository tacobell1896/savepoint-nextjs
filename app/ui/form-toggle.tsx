"use client";

import * as React from "react";

const showForm = false;
function displayForm(showForm: boolean) {
  if ((showForm = true)) {
    return (showForm = false);
  } else {
    return (showForm = true);
  }
}
